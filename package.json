{
  "name": "sendgrid-inbound-parse-webhook-server",
  "version": "1.0.0",
  "description": "Node server to handle SendGrid Inbound Parse webhooks",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": ">=12"
  }
}
